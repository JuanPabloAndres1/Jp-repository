let primernumero = window.prompt ("dame el pirmer numero")
let segundonumero = window.prompt ("dame el segundo numero")

function sumar(primer=0,segundo=0) {
    let primernumero = parseInt(primer)
    let segundonumero = parseInt(segundo)
    let resultado = primernumero + segundonumero
console.log ("el resultado es: " + resultado)
 }

 sumar(primernumero, segundonumero)

 